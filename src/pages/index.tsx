// pages/index.tsx
import Layout from "@/app/layout";

const Home: React.FC = () => {
    return (
        <Layout>
        <h1 className="text-center">Welcome to [Your Company Name]</h1>
        <p>Your trusted partner in [Industry].</p>
        </Layout>
    );
};

export default Home;
